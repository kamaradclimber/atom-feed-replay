FROM golang:1-alpine AS builder
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o /replay-server .

FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
COPY --from=builder /replay-server /replay-server
EXPOSE 8080
ENTRYPOINT ["/replay-server"]
