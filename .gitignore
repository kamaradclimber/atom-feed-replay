# Binaries (root-level only to avoid matching cmd/ subdirectories)
/atom-feed-replay
/yt-feed-builder

# Config (local overrides)
config.yaml
!config.yaml.example
config-yt.yaml
config-replay.yaml

# API key files
youtube-api.key
*.key

# Data directories
data/
yt-data/

# Data directory
data/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
